# 베타 배포 — 웹 (Cloudflare Pages + Supabase)

20~30명 비공개 베타를 링크 하나로 돌리는 설정입니다. 전부 무료 티어 안에 들어갑니다.

구조는 이렇습니다:

```
브라우저 ──► Cloudflare Pages (정적 파일 11MB, PWA)
   │
   └──► Supabase ── 구글 로그인(세션 발급)
                 ├─ saves          저장본 1행 (나만 읽는다)
                 ├─ profiles       공개 한 줄 → 랭킹 · 투기장 상대 · 채팅 명패 · 길드원
                 ├─ guilds         길드 명부 (사람이 만든 것만)
                 ├─ feed_events    실시간 상황
                 └─ chat_messages  전체 · 길드 채팅
```

게임 로직은 **한 줄도 서버로 안 갔습니다.** 서버가 하는 일은 "누구인지 확인",
"저장본 JSON 보관", 그리고 "각자가 올린 것을 모아 나눠 주기" 셋뿐입니다
(`server-design/00-ROADMAP.md` §2 의 원칙 그대로).

---

## 1. Supabase 프로젝트 만들기

1. https://supabase.com → **New project**
   - Region 은 **Northeast Asia (Seoul)** 을 고릅니다. 테스터가 국내면 왕복이 제일 짧습니다.
   - DB 비밀번호는 아무거나 (직접 접속할 일은 없습니다)
2. 만들어지면 **SQL Editor** 로 가서 `supabase/schema.sql` 을 통째로 붙여 넣고 **Run**
   - `saves` 테이블 + RLS 정책 + `push_save()` 함수
   - `profiles` · `guilds` · `feed_events` · `chat_messages` + RLS 정책 + 실시간 전송 설정
   - 여러 번 실행해도 안전합니다

   > ⚠ **이미 배포된 프로젝트라면 이 단계를 다시 밟아야 합니다.** 멀티플레이 표
   > 셋이 새로 생겼습니다. 안 돌리면 랭킹이 나 혼자로 보이고 채팅이 안 갑니다
   > (게임은 그대로 돕니다 — 실패는 조용히 넘어가게 되어 있습니다).

### 구글 로그인 연결

3. **Authentication → Sign In / Providers → Google** 을 켭니다
4. 거기 적힌 **Callback URL** 을 복사합니다 (`https://<프로젝트ref>.supabase.co/auth/v1/callback`)
5. [구글 클라우드 콘솔](https://console.cloud.google.com/apis/credentials) → 쓰고 계신 OAuth
   클라이언트(`329853970665-...`) → **승인된 리디렉션 URI** 에 4번 값을 추가
6. 구글 콘솔의 **클라이언트 ID / 클라이언트 보안 비밀번호**를 Supabase 의 Google provider 칸에 붙여 넣고 저장

> ⚠ 6번의 **보안 비밀번호는 Supabase 에만** 들어갑니다. 앱 코드나 `.env` 에 넣지 마세요.
> 앱은 클라이언트 ID 만 알면 됩니다 (`app.json` 에 이미 있습니다).

### 키 받아오기

7. **Project Settings → Data API** 에서 `Project URL` 과 `anon public` 키를 복사
8. 프로젝트 루트에 `.env` 를 만듭니다 (`.env.example` 참고):

```
EXPO_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
```

`anon` 키는 번들에 박혀서 공개됩니다 — **원래 그런 키입니다.** 실제 통제는 2번에서 건
RLS 가 합니다. `service_role` 키는 RLS 를 무시하므로 절대 넣지 마세요.

---

## 2. Cloudflare Pages 배포

```bash
npx wrangler login        # 브라우저가 열립니다. 한 번만 하면 됩니다
npm run deploy            # 빌드 + 업로드
```

첫 실행 때 프로젝트 생성 여부를 묻습니다 — `player-growth` 로 만들면 됩니다.
끝나면 `https://player-growth.pages.dev` 가 나옵니다.

이후 고칠 때마다 `npm run deploy` 한 줄이면 재배포됩니다.

### 배포 후 한 번 더 — 구글 리디렉션

앱이 로그인 후 돌아올 주소를 구글이 알아야 합니다.
[구글 콘솔](https://console.cloud.google.com/apis/credentials) → OAuth 클라이언트
(`329853970665-...`) 에 아래 두 값을 넣습니다.

| 칸 | 값 |
|---|---|
| 승인된 JavaScript 원본 | `https://player-growth.pages.dev` |
| 승인된 리디렉션 URI | `https://player-growth.pages.dev` |

**끝에 슬래시를 붙이지 마세요.** 앱이 구글에 보내는 값은 `window.location.origin`
그대로이고 (expo-auth-session `SessionUrlProvider.getRedirectUrl`), 거기엔 슬래시가
없습니다. 하나만 달라도 `redirect_uri_mismatch` 가 납니다.

> Supabase 의 콜백 URL(`.../auth/v1/callback`)은 **이 흐름에서는 필요 없습니다.**
> 앱이 구글에서 id_token 을 직접 받아 `signInWithIdToken` 으로 넘기기 때문에
> 브라우저가 Supabase 로 리디렉션되지 않습니다. Supabase 쪽에는 Google provider 를
> 켜고 **Client IDs 칸에 같은 클라이언트 ID** 만 들어 있으면 됩니다 — 그 값으로
> 토큰의 `aud` 를 검증합니다.

> ⚠ **미리보기 URL 로는 로그인이 안 됩니다.** 배포할 때마다
> `https://436e448f.player-growth.pages.dev` 같은 주소가 같이 나오는데, origin 이
> 달라서 구글이 막습니다. 테스터에게는 반드시 `https://player-growth.pages.dev` 를 주세요.

---

## 3. 확인

배포된 링크에서 이 순서로 한 번 밟아 보면 클라우드 저장이 진짜 도는지 알 수 있습니다.

1. 구글로 로그인 → 캐릭터 만들고 아무거나 강화
2. **기타 → 설정 → 클라우드 저장** 이 `연결됨` 인지 확인, **지금 동기화** 누르기
3. 브라우저에서 그 사이트의 데이터를 전부 지우기 (또는 다른 브라우저·시크릿 창)
4. 다시 들어가서 **같은 구글 계정**으로 로그인
5. → 강화해 둔 장비가 그대로 있으면 성공입니다

---

## 동기화가 언제 도는가

| 시점 | 동작 |
|---|---|
| 로그인 직후 | 서버·로컬 중 최신을 고름 (서버가 최신이면 받아서 화면까지 반영) |
| 탭을 가리거나 닫을 때 | 올림 |
| 5분마다 | 올림 |
| 설정의 "지금 동기화" | 즉시 |

충돌은 `rev` 단조 증가로 풉니다. 두 기기에서 번갈아 했다면 **rev 가 큰 쪽(서버)이 이깁니다.**
지는 쪽 저장본은 덮이기 전에 `player-growth/v1.overwritten` 키에 백업으로 남습니다.

## 알아 둘 것

- **`.env` 를 고쳤으면 빌드 캐시를 비워야 합니다.** Metro 는 변환 결과를 캐시하는데,
  자격증명은 빌드 시점에 코드 안으로 박히는 값이라 캐시가 남아 있으면 **옛 값(없음)이
  그대로 실립니다.** 오류도 안 나고 클라우드만 조용히 꺼집니다. `npm run deploy` 와
  `npm run build:web` 은 `--clear` 를 항상 붙여 두었으니 그냥 쓰면 됩니다.
  (`npx expo export` 를 직접 칠 땐 `--clear` 를 꼭 붙이세요)
- **자격증명이 없으면 클라우드가 통째로 꺼진 채 게임이 돕니다.** 로컬 개발할 때 `.env` 없이
  그냥 `npm start` 해도 됩니다 — 지금까지와 똑같이 동작합니다.
- **쿠폰 코드는 배포 빌드에 안 들어갑니다** (`__DEV__` 게이트). `npm start` 로 띄운
  개발 빌드에서는 `iloverakdos` 계열이 그대로 동작합니다.
- **치트는 못 막습니다.** 판정이 전부 클라이언트에 있어서, 저장본을 고치면 골드가 늡니다.
  로드맵 §4 의 판단대로 베타에서는 그냥 둡니다. 다만 이제 랭킹이 **진짜 사람들**이라
  피해가 실제로 갑니다 — RLS 로 "남의 줄은 못 쓴다" 까지만 막아 뒀습니다.
  자기 점수를 부풀리는 건 여전히 가능하니, 이상한 순위가 보이면 대시보드의
  `profiles` 에서 그 행을 지우면 됩니다.
- **Supabase 무료 프로젝트는 7일 동안 요청이 없으면 일시정지됩니다.** 베타 기간에는
  누군가 매일 들어오니 문제없지만, 베타가 끝나고 한참 뒤 다시 켤 땐 대시보드에서
  Restore 를 눌러야 합니다.

---

## 멀티플레이 — 무엇이 실시간인가

| 화면 | 어디서 오는가 | 얼마나 빨리 |
|---|---|---|
| 랭킹 | `profiles` | 남이 올리는 즉시 (소켓). 소켓이 죽으면 최대 60초 |
| 홈 › 실시간 | `feed_events` | 즉시. 소켓이 죽으면 최대 25초 |
| 채팅 | `chat_messages` | 즉시. 소켓이 죽으면 최대 25초 |
| 투기장 상대 | `profiles` | 화면에 들어올 때 한 번 (상대가 접속해 있을 필요 없음) |
| 길드 목록 · 인원 | `guilds` + `profiles` | 즉시. 소켓이 죽으면 최대 60초 |
| 내 기록 올리기 | `profiles` | 30초마다 확인, **바뀌었을 때만** 업로드 |

소켓(Realtime)이 조용히 죽는 경우가 있어서 — 지하철, 사내 프록시, 절전에서
깨어난 브라우저 — 낮은 빈도의 재확인을 같이 돌립니다. 소켓이 살아 있으면
받아 온 게 전부 이미 있는 줄이라 아무 일도 안 일어납니다.

### 길드

**미리 만들어 둔 길드가 없습니다.** 예전엔 280개를 지어내 목록에 깔아 두었는데,
그 안의 길드장·길드원이 전부 없는 사람이었습니다. 지금은 **목록이 비어 있는 채로
시작하고**, 누군가 100골드를 내고 만들어야 첫 줄이 생깁니다.

인원 · 평균 아이템레벨 · 주간 기여도는 `guilds` 표에 **저장하지 않습니다.**
`profiles` 를 세면 나오는 값이라 저장하면 두 곳이 어긋납니다 (탈퇴했는데 인원이
그대로인 식). 프로필은 랭킹 때문에 어차피 다 받아 두므로 추가 질의도 없습니다.

레이드 · 합동 보스의 총 피해와 오늘 출석한 인원, 길드 퀘스트 진행분도 마찬가지로
**길드원들이 각자 올린 값의 합**입니다 (`profiles.guild_stats`). 그래서:

| | 예전 | 지금 |
|---|---|---|
| 보스 체력 | 지어낸 NPC 딜이 늘 채워 줌 → 항상 잡힘 | 안 오면 **정말 못 잡음** |
| 길드 퀘스트 | 목표의 30~60% 가 미리 차 있음 | 0에서 시작 |
| 출석 인원 | "나 외 12명" (가짜) | 오늘 실제로 누른 사람 수 |
| 길드원 명단 | `randomNick()` 12명 | 실제 가입자 |

> ⚠ **옛 저장본의 길드 소속은 풀립니다.** `g37` · `mine` 같은 옛 id 는 가리키는
> 곳이 없어서, 마이그레이션(`STATE_VERSION` 3)이 소속을 비웁니다. 이미 길드를
> 만들었던 테스터는 창설비 100골드를 돌려받지 못합니다 — 자동 환불은 넣지
> 않았습니다 (`merge` 가 새로고침마다 도는 구조라 환불 로직은 중복 지급 위험이
> 큽니다). 필요하면 쿠폰으로 보전해 주세요.

---

## PWA — 홈 화면에 앱처럼

`npm run build:web` 이 빌드 뒤에 `tools/pwa.mjs` 를 돌려 `dist/index.html` 에
manifest·아이콘·서비스 워커 등록을 넣습니다. 따로 할 일은 없습니다.

- **안드로이드/크롬**: 주소창의 "앱 설치" 또는 메뉴 › 홈 화면에 추가
- **아이폰/사파리**: 공유 › 홈 화면에 추가 (사파리는 manifest 를 안 보므로
  `apple-mobile-web-app-*` meta 를 따로 넣어 뒀습니다)

깔고 나면 주소창 없이 전체 화면으로 뜨고, 정적 파일 11MB 는 서비스 워커가
캐시해 두 번째 실행부터 즉시 뜹니다. `index.html` 은 **캐시하지 않습니다** —
캐시하면 새 빌드를 올려도 테스터가 옛 번들을 계속 받습니다.

아이콘을 바꿨다면 `public/sw.js` 의 `VERSION` 을 올리세요. 안 올리면 이미 깐
사람의 워커가 옛 아이콘을 계속 꺼내 줍니다.

---

## 채팅이 "전송 실패" 로 뜬다면

거의 항상 **스키마를 아직 안 올린 것**입니다. 이제 실패하면 채팅창에 사유가
한 줄 뜹니다:

| 채팅창에 뜨는 말 | 뜻 | 할 일 |
|---|---|---|
| 서버에 채팅 표가 아직 없습니다 | `chat_messages` 가 없음 | `supabase/schema.sql` 실행 |
| 이 채널에 쓸 권한이 없습니다 | RLS 가 막음 (길드 채팅인데 길드가 없거나 다름) | 길드 가입 후 재시도 |
| 서버에 연결되어 있지 않습니다 | 자격증명 없음 / 로그인 안 됨 | `.env` 와 로그인 확인 |

표가 올라갔는지는 이걸로 바로 볼 수 있습니다:

```bash
curl -s -o /dev/null -w "%{http_code}
"   "$SUPABASE_URL/rest/v1/chat_messages?select=count"   -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY"
```

`200` 이면 있는 것이고, `404` 면 아직 없는 것입니다.

---

## 서버 세션이 안 열릴 때

증상: 구글 로그인은 되는데 랭킹·채팅·길드·클라우드 저장이 전부 안 되고,
설정 화면에 "구글 계정에는 로그인됐지만 서버 세션이 없습니다" 가 뜬다.

**게임 계정과 Supabase 세션은 별개다.** 구글 로그인은 게임 계정을 만들고,
Supabase 세션은 그걸로 따로 발급받는다. 둘이 어긋나면 겉보기엔 멀쩡한데
서버를 쓰는 기능만 전부 죽는다.

고치는 길은 **기타 › 설정 › 클라우드 저장 › "서버 세션 다시 열기"** 하나다.
구글 화면으로 갔다가 돌아오고, 돌아오면 "연결됨" 이 된다.

> 이 버튼은 **리디렉션**을 쓴다 (팝업이 아니다). 팝업 방식은 브라우저가 막으면
> 아무 일도 안 일어난 것처럼 끝나서, 눌러도 시도 기록조차 안 남는 상태에 갇힌다.

### 리디렉션이 엉뚱한 곳으로 가면

Supabase → **Authentication → URL Configuration** 을 확인한다:

| 칸 | 값 |
|---|---|
| Site URL | `https://player-growth.pages.dev` |
| Redirect URLs | `https://player-growth.pages.dev/**` |

여기 없는 주소로는 못 돌려보낸다 — 대신 Site URL 로 보내 버리므로, 기본값
(`http://localhost:3000`)이 남아 있으면 죽은 페이지로 떨어진다.

그리고 [구글 콘솔](https://console.cloud.google.com/apis/credentials)의 OAuth
클라이언트에 **승인된 리디렉션 URI** 로 Supabase 콜백이 들어 있어야 한다:

```
https://<프로젝트ref>.supabase.co/auth/v1/callback
```

### 그래도 안 되면

설정 화면의 진단 칸에 **실패 사유 원문**과, 이 빌드가 쓰는 **클라이언트 ID**,
구글 토큰의 **aud** 가 같이 뜬다. 셋을 Supabase 의 Google provider 설정과
나란히 놓고 대조하면 어긋난 곳이 바로 보인다.
