# 플레이어 키우기 — 개발 문서

`readme.md` 기획서 v1.0 을 React Native (Expo) 로 구현한 프로토타입.

## 실행

```bash
npm install
npm start          # Expo Go 로 QR 스캔 (네이티브 빌드 불필요)
npm run web        # 브라우저에서 바로 확인 — 데모용으로 편함
```

**Expo Go 에서 그대로 돕니다.** 네이티브 모듈은 Expo Go 에 이미 포함된 것만 씁니다
(reanimated / gesture-handler / screens / safe-area / async-storage / haptics).
Skia·MMKV 를 쓰지 않은 건 의도적입니다 — 흑백 2색 도트라 `View` 만으로 충분하고,
그 대가로 개발 빌드 없이 QR 스캔만으로 팀 전체가 돌려볼 수 있습니다.

## 검증

```bash
npm run typecheck  # tsc --noEmit
npm run smoke      # 코어 로직 전 모듈 스모크 테스트 (1,000여 개 단언)
python tools/check-cycles.py   # 순환 import (빌드는 통과하고 실행에서 터지는 것)
npm run sim        # 밸런스 시뮬레이터 — 강화 20,000회 × 티어별
```

`sim` 은 UI 없이 확률/비용만 수만 번 돌립니다. 기획서 §13 의 설계 원칙이
숫자로 실제 지켜지는지 확인하는 용도입니다 (아래 "밸런스 실측" 참고).

## 구조

```
App.tsx                  네비게이션 셸 · 시간 경과 정산 드라이버
src/
  core/                  ★ 순수 TypeScript — RN 임포트 0개
    types.ts             부위 21종 · 슬롯 16칸 · 도메인 타입
    currency.ts          골드/실버/쿠퍼 (내부는 항상 정수 쿠퍼)
    tiers.ts             티어 테이블 · 아이템레벨 · 내구도 보정
    enhance.ts           강화 확률/비용/주문서/승급        (§4)
    economy.ts           판매가 · 수리비 · 대출 · 회생      (§3-3, §7-7~9, §10)
    combat.ts            체력 · 퀘스트 · 투기장 · 탐험 · 탑  (§7-3~6, §9)
    sutda.ts             섯다 20장 · 족보 판정             (§7-1)
    rush.ts              크리처 러쉬 · 배당 역산 · 전투 로그 (§7-1)
    rumor.ts             선술집 소문                      (§8-2)
    collection.ts        도감 210칸 · 완성 보상            (§11)
    titles.ts            칭호 11종 · 효과 · 통계           (§12)
    events.ts            이벤트 (출석체크)
    lottery.ts           복권 — 오후 8시 추첨, 시드 고정 결과
    parttime.ts          아르바이트
    feed.ts              실시간 이벤트 피드 (닉네임·이벤트 생성)
    chat.ts              채팅 도메인 + ChatTransport 인터페이스
    profanity.ts         욕설 필터 — 닉네임은 막고, 채팅은 별표로 가린다
    rng.ts               시드 고정 PRNG
    __sim__.ts / __smoke__.ts
  state/                 ★ 아래 "상태 계층" 참고 — 층이 나뉘어 있고 순환이 없다
    types.ts             상태·액션의 **모양** (타입만, 실행 코드 0줄)
    initial.ts           새 계정의 기본값
    migrate.ts           옛 저장본 보정 — 저장본은 절대 그대로 믿지 않는다
    helpers.ts           GameState 를 인자로 받는 순수 잔손질 (스토어를 모른다)
    slices/              액션 아흔일곱 개를 열 뭉치로 (gear · battle · guild …)
    store.ts             조립 + 영속화 미들웨어. 저장 설정은 여기에만 있다
    selectors.ts         화면이 읽어 가는 셀렉터·훅 (여기만 useGame 을 안다)
    storage.ts           AsyncStorage 디바운스 래퍼 — 유실 방어의 1차 방어선
    cloudSave.ts         계정에 저장본을 올리고 내린다 (rev 로 충돌 판정)
    poller.ts            앞에 있을 때만 도는 재확인 타이머
    live.ts              이벤트 피드 · 채팅 (게임 스토어와 분리된 별도 스토어)
  ui/                    theme · 도트 스프라이트 렌더러 · 원자 컴포넌트 · 강화 연출
  screens/               홈 / 돈 벌기 / 마을 / 컬렉션 / 기타
```

### 왜 core 를 분리했나

1. **밸런스를 실측할 수 있다** — 확률표만 보고는 "+15 까지 얼마 드는가"를 알 수 없습니다.
2. **치팅 대응 시 서버로 그대로 옮길 수 있다** — RN 의존이 없습니다.
3. **UI 없이 테스트된다** — `npm run smoke` 가 3초 안에 끝납니다.

### 상태 계층 (2026-08 정리)

`store.ts` 한 파일에 3,652줄이 있었습니다. 액션 하나를 고치러 들어가면 관계없는
스무 개를 지나야 했고, "저장되는 값이 뭐였더라" 를 보려면 그 앞을 스크롤로 넘어야
했습니다. 지금은 층이 나뉘어 있고 **화살표가 한 방향으로만** 흐릅니다.

```
types.ts        타입만 — 실행 코드가 없어 누가 먼저 로드해도 안전
   ↑
helpers.ts      순수 함수. 상태를 **인자로 받는다** (useGame 을 모른다)
   ↑
slices/*.ts     액션 뭉치. set/get 만 받고 스토어를 만들지 않는다
   ↑
store.ts        조립 + persist. 저장·마이그레이션은 여기에만 있다
   ↑
selectors.ts    useGame 을 아는 유일한 층
```

나눠도 **스토어는 여전히 하나**입니다. 뭉치들은 `get()` 으로 서로의 액션을 그대로
부르고, 저장본의 모양도 그대로입니다 (마이그레이션 불필요).

⚠ 순환 import 는 타입 검사도 빌드도 통과하고 **실행할 때** "상대 모듈의 값이
undefined" 로 터집니다. 그래서 기계가 봅니다:

```bash
python tools/check-cycles.py   # 순환이 있으면 종료코드 1
```

## 도트 아트 파이프라인

`assets/lending-image/` 에 받은 마젠타 경계 시트 28장 → `assets/sprites/` 196장.

```bash
python3 tools/slice.py           # 전체 재생성
python3 tools/slice.py avatar    # 한 세트만
```

`tools/sprites.config.json` 이 시트마다 격자·라벨·후처리를 지정합니다.

| 옵션 | 용도 |
|---|---|
| `expect: [cols, rows]` | 검출된 격자와 다르면 경고 (시트 오인 방지) |
| `pickRows: [1]` | Gemini 가 변형 행을 덧붙여 낸 경우 쓸 행만 고름 |
| `drop: [12]` | 빈 칸/워터마크 칸 제외 |
| `cropBottom: 0.15` | **셀 안에 박힌 글자 제거** (하단 비율) |
| `invert: true` | 흰 배경 + 검은 글리프로 나온 시트 뒤집기 |
| `killCorner: true` | 우하단 Gemini 워터마크 지우기 |
| `labels: [...]` | 셀 → 파일명 (없으면 `01`, `02`…) |

처리 순서: 마젠타 격자 검출 → 셀 크롭 → 하단 글자 제거 → 이진화(임계 128) →
반전/워터마크 처리 → 검은 여백 트림 → 최근접 축소(상한 192px) →
**흰 픽셀 + 투명 배경 PNG**.

투명 배경으로 저장하는 이유는 `tintColor` 로 색을 갈아끼울 수 있어서입니다 —
흑백 2색이라 선택된 칸(반전 배경) 위에 같은 에셋을 검정으로 올릴 수 있습니다.

`src/ui/spriteAssets.ts` 는 **자동 생성**됩니다 (Metro 가 정적 `require` 만 해석하므로).
직접 수정하면 다음 실행에서 덮어씁니다.

`<Sprite set="..." name="..." fallback={...} />` 로 씁니다. 에셋이 없으면 `fallback`
(기존 ASCII 스프라이트)으로 자동 대체되므로, 아트가 순차적으로 들어와도 화면이 비지 않습니다.

## 오버레이는 ScrollView 밖에서 렌더해야 한다 (함정)

화면들이 `<Screen>`(= ScrollView) 안이라, 오버레이를 화면 컴포넌트 안에 두고
`position: absolute; top: 0; bottom: 0` 을 주면 **좌표가 스크롤 콘텐츠 기준**이 됩니다.
콘텐츠가 뷰포트보다 훨씬 길어서 `justifyContent: 'center'` 가 화면 밖을 가리킵니다.
실제로 전투 연출과 도감 팝업에서 두 번 겪었습니다.

해결책 두 가지를 상황에 맞게 씁니다.

| 방식 | 쓰는 곳 | 이유 |
|---|---|---|
| **앱 루트 호스트 + 스토어** | 전투 연출(`CombatFxHost`), 채팅, 토스트 | 여러 화면에서 띄우고 상태를 공유 |
| **`<Modal transparent>`** | 도감 팝업(`InfoBubble`) | 한 화면에서만 쓰고 자기 데이터로 충분 |

## 도감 팝업 (InfoBubble)

컬렉션에서 칸을 누르면 말풍선이 뜹니다.

- **획득한 칸**: 티어, 기본 아이템레벨, 강화 1회당 상승, +15 도달치,
  +0 판매가, +1 강화 비용, 다음 티어 승급 비용
- **미획득 `?` 칸**: 어떤 장비인지 + **획득 조건을 단계별로** 표시
  - 티어 1 → 상점에서 1쿠퍼
  - 티어 2~10 → 이전 티어 +15 강화 후 승급 (승급 비용까지)
  - 장인 → 보스의탑 50층 재료 100개 + 500골드
  - 1단계가 이미 충족되면 반전 뱃지로 표시하고, 아니면 "아래 티어부터" 안내

계산은 `src/core/itemInfo.ts`(순수 함수)가 합니다 — `itemSpec()` / `unlockOf()`.

## 전투 연출 (§1 — 무기별 공격 모션)

`src/ui/CombatFx.tsx`. 투기장·탐험·보스의탑에서 도전하면 1.9초 연출이 재생됩니다.

```
대치 → 공격 프레임 1·2·3 → 결과
       (580ms 에 플래시 + 흔들림 + 햅틱)
```

무기 종류가 모션을 고릅니다 — `WEAPON_MOTION[kind]` 이 `thrust / slash / smash /
shoot / cast` 를 주고, 그게 그대로 `assets/sprites/pl_attack/{motion}_{1..3}` 키가
됩니다. 창은 찌르고 망치는 내려칩니다. 화면을 누르면 건너뜁니다.

**연출은 앱 루트(`CombatFxHost`)에서 렌더합니다.** 화면의 `ScrollView` 안에 두면
absolute 좌표가 스크롤 콘텐츠 기준이 되어, 스크롤을 내린 상태에서는 화면 밖에
그려집니다. 상태는 `useBattle` 스토어에 있고 화면은 `show()` 만 호출합니다.

전투 결과는 이미 확률로 정해져 있고 이 오버레이는 그 결과에 도달하는 **연출**입니다.
스토어 액션이 먼저 상태를 바꾸므로 HUD 소지금은 연출보다 살짝 먼저 움직입니다.

## 투기장 로고 (아바타)

12종 초상을 **플레이어가 고르는 투기장 로고**로 만들었습니다 (기타 › 프로필).
`src/core/avatars.ts` 가 도메인, `src/screens/AvatarPicker.tsx` 가 선택 UI 입니다.

고스트 상대에게도 같은 세트에서 얼굴을 배정합니다 — `avatarForName()` 이 이름을
해시해서 고르므로 같은 이름이면 항상 같은 얼굴이 나옵니다.

실제 PvP 가 붙으면 `Ghost.avatar` 를 상대가 고른 `avatar` 로 채우면 끝입니다.
UI 는 손댈 필요가 없습니다.

## 실시간 계층 (피드 · 채팅)

게임 스토어와 **별도 스토어**(`src/state/live.ts`)로 분리했습니다.
수 초마다 갱신되는 소셜 데이터가 게임 화면 전체를 리렌더시키면 안 되기 때문입니다.

### 이벤트 피드 (홈 "실시간" 패널)

- 다른 플레이어 이벤트는 현재 **시뮬레이션**(`src/core/feed.ts`)이지만,
  `FeedEvent` 스키마는 서버가 밀어줄 형태 그대로입니다 — 소스만 갈아끼우면 됩니다.
- **내 실제 이벤트도 같은 피드에 올라갑니다** (강화 성공/파괴, 승급, 투기장 승리,
  탐험·탑 클리어, 장인 제련, 3금융 대출, 회생, 도박 적중). `나` 태그로 구분됩니다.
- 희귀 이벤트(+13 이상 강화/파괴, 탑 50층, 장인 제련, 대박)는 흑백 반전으로 강조합니다.

### 채팅

우측 하단에 화면을 따라다니는 말풍선 버튼(`ChatFab`)이 모든 탭 위에 떠 있습니다.
탭바 위(`bottom: 78`)에 고정되어 스크롤·화면 전환과 무관하게 같은 자리를 지킵니다.

- **안 읽음 뱃지**: `formatUnread()` — `0`은 미표시, `1~100`은 숫자 그대로,
  `101 이상`은 `100+`. 채팅창이 열려 있는 동안 들어온 메시지는 안 읽음으로 세지 않습니다.
- **스크롤백 100개** (`CHAT_HISTORY_MAX`). 오래된 메시지는 잘라냅니다.

**실시간 서버는 아직 안 붙었습니다.** 대신 갈아끼울 자리를 만들어 뒀습니다:

```ts
// src/core/chat.ts
export interface ChatTransport {
  connect(onMessage: (m: ChatMessage) => void): () => void;  // 정리 함수 반환
  send(text: string): Promise<void>;
  readonly name: string;
}
```

지금은 `createMockTransport()` 가 꽂혀 있어 랜덤 간격으로 가짜 메시지가 들어옵니다.
WebSocket 구현을 만들어 `useChat.getState().setTransport(ws)` 로 교체하면
UI 는 한 줄도 안 고치고 그대로 갑니다.

## 이벤트 / 부수입 (출석체크 · 아르바이트 · 복권)

| 기능 | 위치 | 규칙 | 코어 |
|---|---|---|---|
| 출석체크 | 기타 › 이벤트 | 하루 1회, 5실버. 연속·누적 출석 기록 | `core/events.ts` |
| 아르바이트 | 돈 벌기 › 노동 | 1회 체력 1~5 랜덤, 10쿠퍼~1실버 랜덤. 5% 확률 팁(×1.5) | `core/parttime.ts` |
| 복권상점 | 돈 벌기 › 도박 | 1장 5실버, 회차당 10장, **매일 오후 8시 추첨** | `core/lottery.ts` |

**아르바이트는 체력 5 이상일 때만** 받습니다. 소모치를 굴린 뒤에 체력이 모자라 막히면
억울하고, 체력을 1로 유지하며 클릭하는 편법(1체력에 평균 55쿠퍼)도 막힙니다.

**복권 결과는 `(회차 키, 발급번호)` 시드로 고정**됩니다. 구매 즉시 결과가 결정되어
있지만 오후 8시까지 공개되지 않으며, 앱을 껐다 켜도 결과가 바뀌지 않습니다.
회차 키는 "다음 오후 8시" 의 날짜라, 구매 한도 10장은 20시→20시 창을 기준으로 셉니다.

### 복권 환급률 (조정 완료)

처음 지정된 상금(1등 5,000골드 / 2등 1,000골드 / 3등 50골드 / 4등 5골드 / 5등 50실버)은
**기대값이 티켓 가격의 6배**였습니다. 각 등수가 티켓값(5실버)만큼을 기대값에 기여해
총 30실버가 되고, 하루 10장(50실버)이면 기대 순익 2골드 50실버 — 사면 살수록 이득인
확정 수입원이었습니다.

**확률은 그대로 두고 상금만 재배치**했습니다. 확률이 ÷10 될 때마다 상금이 ×10 이 되도록
맞춰 각 등수의 기여를 50쿠퍼로 균등화했습니다.

| 등수 | 확률 | 상금 | 기대값 기여 |
|---|---|---|---|
| 1등 | 0.001% | 500골드 | 50쿠퍼 |
| 2등 | 0.010% | 50골드 | 50쿠퍼 |
| 3등 | 0.100% | 5골드 | 50쿠퍼 |
| 4등 | 1.000% | 50실버 | 50쿠퍼 |
| 5등 | 10.000% | 5실버 | 50쿠퍼 |
| | | **1장 기대회수** | **2실버 50쿠퍼** |

**환급률 50%** (실제 복권과 같은 수준). 하루 10장이면 기대 손익 −25실버.
`payoutRatio()` 로 노출되고 `npm run sim` 8번이 매번 출력합니다.

## ⚠ 아이템레벨 = 합 (기획서 §3-1 에서 변경)

기획서 §3-1 은 **플레이어 아이템레벨 = 착용 16슬롯의 평균**으로 정의하지만,
평균은 슬롯 하나를 강화해도 1/16 만 올라 강화가 반영되지 않는 것처럼 보입니다.
(`낡은 검 +6` = 아이템레벨 16 → 평균 1. 강화 6번을 해야 1이 오릅니다.)

그래서 **합**으로 변경했습니다.

```ts
// src/core/tiers.ts
playerIlvl(eq) = Σ itemLevel(착용 16슬롯)     // 빈 슬롯은 0
```

### 곡선을 같은 단위로 올렸습니다 (중요)

§7-3~7-6 의 권장 템렙·승률 계수·보상 계수가 **전부 평균 기준**으로 짜여 있습니다.
합으로 바꾸고 곡선을 그대로 두면 플레이어가 전 콘텐츠를 한 번에 뚫습니다.
그래서 곡선 쪽을 `SLOT_COUNT`(=16) 배로 올려 **상대 난이도를 기획서와 동일하게** 유지했습니다.

| 항목 | 기획서(평균) | 구현(합) |
|---|---|---|
| 탐험 챕터 n 권장 | `10 + n × 11` | `(10 + n × 11) × 16 × CURVE_SCALE` |
| 챕터 100 권장 | 1,110 | **18,615** = `maxSetIlvl()` |
| 보스의탑 층 n 권장 | `n × 22` | 선형 폐기 → **지수 곡선** (1층 340 → 50층 `towerTopIlvl()`) |
| 50층 권장 | 1,100 | **18,764** = 10티어 +12 풀셋 + A~S 정령석 |
| 승률 (퀘스트) | 평균 기준 차이식 | 입력을 `toAvg()` 로 환산해 그대로 사용 (보상 배수 역산 + 장비 개선분) |
| 승률 (탐험 · 탑 · 투기장) | 차이식 (0.3 / 0.6) | **폐기 → 비율식** `combat.ilvlWinRate` (아래) |
| 보상·보증금 계수 | 평균 기준 | `toAvg()` 환산 (안 하면 16배가 되어 경제 붕괴) |

### 승률은 차이가 아니라 **비율**입니다 (2026-08 개정)

탐험 · 보스의탑 · 투기장이 전부 `combat.ilvlWinRate(내 템렙, 상대 템렙)` 하나를 씁니다.

| 내 템렙 / 상대 템렙 | 0.5 | 0.67 (=1/1.5) | 0.8 | **1.0** | 1.25 | **1.5** | 2.0 |
|---|---|---|---|---|---|---|---|
| 승률 | 8.5% | **20.0%** | 31.8% | **50.0%** | 80.6% | **93.0%** | 97%(상한) |

예전 식은 `50% + (내 템렙 − 상대 템렙) × 계수` 였습니다. 차이식은 스케일을 못 따라갑니다 —
템렙 30 대 60(두 배 차이)이 9%p 밖에 안 벌어지는데 템렙 3,000 대 3,100(3% 차이)은 30%p 가
벌어집니다. 초반엔 장비를 올려도 승률이 안 움직이고, 후반엔 한 칸 강화가 승패를 뒤집습니다.

비율식은 로그 비율 위의 로지스틱이고, 위아래 기울기가 **다릅니다**(93% vs 20%).
대칭으로 두면 1.5배 열세가 7% 가 되어 "장비를 덜 갖추면 아예 못 한다" 가 됩니다.
20% 는 "불리하지만 운이 따르면 뚫린다" 는 자리입니다.

⚠ **심연은 이 곡선을 쓰지 않습니다** (`abyss.descentPass`). 심연은 같은 판정을 20번
연달아 통과해야 해서, 한 방 판정용 가파른 곡선을 물리면 최고 세트의 완주율이
15% → 2.6% 로 무너집니다. 심연만 예전의 완만한 차이식을 자기 곡선으로 들고 갑니다.

### ⚠ 이 표의 숫자를 상수로 옮겨 쓰지 마세요

위 값은 **부위 가중치(`KIND_ILVL_WEIGHT` = weapon 1.7 / armor 1.2 / acc 0.9)에서 파생**됩니다.
가중치를 건드리면 전부 움직이므로, 코드는 `maxSetIlvl()` / `fullSetIlvl()` / `towerTopIlvl()` 을
호출해야 하고 문서의 숫자는 **그 시점 실측값일 뿐**입니다.

- 초기 균등 가중치 시절의 값이 **17,760** 이었고, 그 값이 한동안 이 문서와 코드 여러 곳에
  상수로 박혀 있었습니다 (`combat.ts:LEGACY_MAX_SET` 이 그 잔재이며, 지금은
  `CURVE_SCALE = maxSetIlvl() / LEGACY_MAX_SET` 로 비례 보정만 합니다).
- 보스의탑은 선형 곡선이 초반에 너무 가팔라 **지수 곡선으로 교체**되었습니다.
  `TOWER_FLOORS` 가 지수의 분모에 들어가므로 **층수를 늘리면 기존 층이 전부 쉬워집니다**
  (50 → 65 로 바꾸면 50층이 −61%). 탑 확장은 곡선 재설계가 선행되어야 합니다.

`npm run sim` 6번 항목이 이 보존을 검증합니다 — 용린 +15 풀셋의 챕터 100 승률이
정확히 **50.0%** 로 나와야 정상입니다. 스모크 테스트에도 같은 단언이 있고
(`exploreRecIlvl(100) === maxSetIlvl()`), 가중치를 바꾸면 그 단언이 먼저 깨집니다.

새 값 감각:

| 장비 상태 | 아이템레벨(합) |
|---|---|
| 낡은 검 +0 만 착용 | 10 |
| 낡은 검 +6 만 착용 | 16 |
| 티어 1 +0 풀셋 (15쿠퍼) | 160 |
| 티어 1 +15 풀셋 | 432 |
| 용린 +15 풀셋 | **18,615** (`maxSetIlvl()`) |

홈 화면은 라벨을 `아이템레벨 (착용 16슬롯 합)` 로 명시하고, 빈 칸이 있으면
**"빈 칸을 최하급으로 채우면 10 → 160"** 안내 패널과 상점 이동 버튼을 띄웁니다 —
초반에 체감을 가장 크게 바꾸는 행동이 강화가 아니라 빈 칸 채우기라서입니다.

## 저장본은 어떻게 지켜지는가 (2026-08)

세 겹입니다. **각 겹이 다른 사고를 막습니다** — 하나로 뭉뚱그리면 안 됩니다.

| 겹 | 파일 | 막는 사고 |
|---|---|---|
| 메모리 → 디스크 | `state/storage.ts` | 쓰기 실패 · 순서 역전 · 탭 닫기 |
| 디스크 → 서버 | `state/cloudSave.ts` | 브라우저 데이터 삭제 · 기기 변경 |
| 무엇을 믿을까 | `state/migrate.ts` | 낡거나 망가진 저장본 |

쓰기는 2초씩 모아서 내립니다 (상태가 초 단위로 바뀌어서 매번 쓰면 저장이 병목입니다).
모아 두는 **그 2초 동안은 값이 오직 메모리에만** 있고, 실제로 유실이 세 군데서
났습니다. 지금은 다음 규칙으로 막습니다.

1. **읽으면 최신이 나온다.** 아직 디스크에 못 간 값이라도 `getItem` 이 그걸
   돌려줍니다. 안 그러면 클라우드 업로드가 최대 2초 낡은 저장본을 올리고,
   그게 rev 를 올려 서버에 박혀 **다른 기기가 그 낡은 것을 정본으로 받아 갑니다.**
2. **쓰기가 실패해도 안 버린다.** 예전에는 보낼 목록을 먼저 비우고 썼기 때문에
   한 번 실패하면 그 값이 증발했습니다. 지금은 되돌려 놓고 다음 flush 가 재시도합니다.
3. **늦게 끝난 쓰기가 최신을 덮지 않는다.** flush 는 한 번에 하나만 돕니다.
4. **받아온 것이 밀린 것에 덮이지 않는다.** `pull()` 은 서버본을 깔기 전에 그 키의
   밀린 쓰기를 **버립니다** (`dropPending`). 서버 왕복 사이에도 게임은 돌아서
   반드시 한 번은 저장이 밀리는데, 그냥 두면 2초 뒤에 방금 받아온 캐릭터를 덮습니다.
   되읽기(`rehydrate`)도 밀린 값을 먼저 보므로 화면까지 옛것으로 돌아갔습니다.
   **클라우드 세이브의 존재 이유 그 자체인 경로**라 여기서 유실이 나면 안 됩니다.
5. **탭을 닫아도 남는다.** `pagehide` 에서 한 번 더 내립니다 (iOS 사파리는
   `beforeunload` 를 안 줍니다). 웹의 AsyncStorage 는 localStorage 를 동기로
   두들기므로 그 순간에 불러도 실제로 남습니다.

넷은 시나리오 테스트로 잠가 두었습니다 — 고치기 전에 **먼저 재현시켰고**, 넷 다
실패하는 것을 보고 나서 고쳤습니다.

## 서버를 언제 두들기는가

재확인 타이머(채팅·피드 25초, 순위표·길드 명부·투기장 피격 60초)는 전부
`state/poller.ts` 를 지납니다. 탭이 숨겨져 있으면 **멈추고, 돌아오면 곧바로 한 번**
받습니다. 절약이자 동시에 체감 개선입니다 — 예전에는 돌아와서 최대 60초를 더
기다려야 새 글이 보였습니다.

길드 명부는 행 하나가 바뀔 때마다 목록 전체(200줄)를 다시 받으므로 `coalesce` 로
묶습니다. 레이드 정산 직후처럼 여러 길드가 같은 순간에 갱신되면 예전에는 같은
200줄을 그 수만큼 받아 왔습니다.

## 없앤 것 — 주식장 (2026-08)

주식 15종 + 파생상품 2종 + 코인 5종을 **통째로** 걷어냈습니다. `core/stock.ts`,
`screens/money/StockScreen.tsx`, 지도의 주식장, 매초 도는 시세 틱, 선술집의 주식
속보, 순자산의 '변동자산' 항목까지 전부입니다.

### 들고 있던 종목은 돈으로 돌려줬습니다

콘텐츠를 없애는 건 기획의 선택이지만, 그 김에 사람이 모아 둔 재산까지 사라지는 건
다른 얘기입니다. 주식에 전 재산을 넣어 둔 사람에게는 그게 그냥 전 재산 소멸입니다.

`state/migrate.ts` 의 `marketPayout` 이 **마지막 시세 그대로** 정산해 소지금에
넣습니다. 손해도 이익도 만들지 않습니다.

- 저장본이 가격(`stocks[id].price`)과 수량(`holdings[id].shares`)을 **둘 다** 들고
  있어서, `core/stock.ts` 가 사라진 뒤에도 계산에 필요한 건 저장본 안에 다 있습니다.
  죽은 모듈을 시세표 때문에 붙잡고 있지 않아도 됩니다.
- ⚠ **두 번 주면 안 됩니다.** 마이그레이션은 저장본을 읽을 때마다 돕니다.
  `marketClosed` 세대(`state/initial.ts` 의 `MARKET_CLOSE_SEQ`)로 한 번만 돌게
  잠갔습니다. 무한 증식은 유실만큼이나 나쁩니다.
- 저장본은 손댈 수 있으므로 수량·가격 모두 유한한 양수만 세고 총액에 상한을 둡니다.
- `ui/MarketClosedPopup.tsx` 이 **닫아야 사라지는 팝업**으로 한 번 알립니다.
  토스트로 스쳐 지나가면 "돈이 왜 늘었지" 가 되고, 그건 알린 게 아닙니다.

### 칭호 둘은 남겼습니다

단타왕(day_trader)·존버(coin_believer)는 더 얻을 수 없지만 **목록에서 지우지
않았습니다.** 지우면 이미 가진 사람의 저장본에서 검증에 걸려 조용히 사라지고,
장착 중이었다면 칭호 칸이 빕니다. 없앤 건 콘텐츠지 그 사람이 한 일이 아닙니다.

효과는 뗐습니다(붙일 곳이 없어졌고, 없는 효과는 칭호 칸만 축냅니다). 대신
`TitleDef.legacy` 로 표시해 화면이 "지금은 얻을 수 없는 칭호" 라고 말합니다.
스모크의 "효과 없는 칭호가 없다" 검사는 **"효과 없는 칭호는 폐지된 것뿐이다"** 로
바뀌었습니다 — 새 칭호를 효과 없이 추가하면 여전히 걸립니다.

### 지도

광장의 가운데 칸(x=36)이 은행·선술집으로 내려가는 **세로 기둥**이었습니다.
주식장이 거기 있었으므로 오락실이 그 자리를 이어받았습니다. 안 옮겼으면 광장에서
아래 구역으로 가는 길이 끊깁니다.

## 없앤 것 — 은행 (2026-08)

대출·회생·연체를 통째로 걷어냈습니다. `screens/town/BankScreen.tsx`,
`money/LoanScreen.tsx`, `money/RehabScreen.tsx`, `core/economy` 의 대출·회생 구획,
지도의 은행, 순자산의 '부채' 항목까지 전부입니다.

### 담보로 잡혀 있던 장비는 창고로 돌려줬습니다

대출은 **담보를 맡기고** 돈을 빌리는 것이라, 은행을 없애면 그 장비가 갈 곳이
없습니다. 그냥 두면 저장본에만 남고 화면 어디에도 안 나오는 유령이 됩니다 —
사람 입장에서는 장비가 사라진 것이고, 담보로 잡히는 건 대개 제일 비싼 물건입니다.

`state/migrate.ts` 의 `loanCollateral` 이 담보를 창고로 옮기고 남은 빚을 지웁니다.

- **빚은 탕감합니다.** 갚을 창구가 없어졌으니 갚으라고 할 수 없습니다. 원금을 도로
  걷는 것도 방법이지만 그 돈은 이미 강화에 썼을 것이고, 그러면 소지금이 음수가
  되거나 장비를 팔아야 합니다 — **우리가 없앤 콘텐츠 때문에** 사람이 손해를 보는
  건 앞뒤가 안 맞습니다. 유리한 쪽으로 잘못하는 편이 낫습니다.
- ⚠ **두 번 돌려주면 안 됩니다.** `bankClosed` 세대(`state/initial.ts` 의
  `BANK_CLOSE_SEQ`)로 한 번만 돌게 잠갔습니다. 안 그러면 켤 때마다 담보가
  창고에 하나씩 복제됩니다.
- 담보도 `fixItem` 을 통과시킵니다 — 담보라고 더 믿을 이유가 없습니다.

### 연체(신용불량)는 반드시 풀어야 했습니다

`delinquentUntil` 은 **퀘스트와 투기장 이용을 막는** 상태였고, 푸는 유일한 방법이
은행의 회생 절차였습니다. 은행만 없애고 이 필드를 두면 연체 중이던 사람은
영구히 잠깁니다. 필드째 지웠습니다.

그래서 `effectsOf(title, delinquent)` 의 두 번째 인자도 없앴습니다 — 연체가 없으니
언제나 false 였습니다.

### 칭호 둘도 폐지 처리

철벽 지갑(iron_wallet)·채무불이행자(defaulter)는 조건과 효과가 둘 다 은행을
가리켰습니다. 주식장 때와 같은 처리 — 목록에는 남기고(`legacy`) 효과만 뗐습니다.

### 지도

중심가도 세 칸짜리 가로줄이고 가운데 칸(x=36)이 광장(오락실)과 가게(선술집)를 잇는
**세로 기둥**이었습니다. 은행이 거기 있었으므로 모험가사무소가 그 자리를
이어받았습니다. 안 옮겼으면 마을이 위아래로 끊깁니다.

## 착용 칸 10칸 (2026-08)

칸 수가 두 번 줄었습니다: **16 → 13 → 10.**

| 단계 | 없앤 것 |
|---|---|
| 16 → 13 | 견갑·장갑·신발의 **좌우 짝** |
| 13 → 10 | **견갑** 자체, 귀걸이·반지의 **둘째 칸** |

이유는 매번 같습니다 — 두 칸이 **같은 물건을 받고 같은 값을 줬습니다.** 왼쪽 신발과
오른쪽 신발, 귀걸이 1과 귀걸이 2는 강화할 때 똑같은 걸 두 번 하는 것 말고
달라지는 게 없었습니다. 칸이 줄면 한 칸에 들이는 공이 그만큼 커집니다.

흉갑은 **갑옷**으로 이름을 바꿨습니다. 부위 이름이 전부 낯선 한자어인데 몸통만이라도
바로 읽히는 편이 낫습니다.

### 빠진 칸의 장비는 창고로

`LEGACY_SLOT` → `fixEquipped` 가 옛 이름을 새 칸으로 옮기고, **갈 곳이 없는 것은
창고로 내려보냅니다.** 값을 치르고 강화까지 해 둔 물건이라 칸이 줄었다는 이유로
사라지면 그건 유실입니다.

### ⚠ 칸 수를 박아 둔 상수가 조용히 죽습니다

이번에 **두 개**가 이미 죽어 있었습니다. 둘 다 13칸이 된 시점부터 달성 불가였는데
아무도 몰랐습니다.

| 자리 | 죽어 있던 이유 |
|---|---|
| `set_keeper` 칭호 | `maxSetCount >= 16` — 세트는 슬롯 수를 못 넘습니다 |
| `SET_STEPS` 마지막 단계 | `count: 13` 을 다시 박아 둠 |

둘 다 **슬롯 수에서 유도**하게 고쳤습니다 (`SLOT_COUNT` · `SLOT_IDS.length`).
칸 수에 연동되는 값을 새로 만들 때는 숫자를 적지 말 것.

### 보상 곡선도 따라 움직입니다

템렙 총합이 내려가면서 보스의탑 1→2층 격차가 150쿠퍼로 주저앉았습니다
("한 층 오르면 5실버는 더" 라는 약속이 그 한 칸에서 깨짐). 계단의 출발점을
**1층이 실제로 주는 값**에 맞춰 고쳤습니다 — 칸 수가 또 바뀌어도 안 어긋납니다.

### 아이템레벨 표

티어별·강화별 실제 수치는 [docs/ITEM_LEVEL_TABLE.md](docs/ITEM_LEVEL_TABLE.md) 에
있습니다. 손으로 적은 표가 아니라 `tools/gen-ilvl.js` 가 **실제 코드에서 뽑습니다** —
공식이 바뀌면 다시 돌리면 됩니다.

표는 전부 **풀셋(10칸) 총합** 기준입니다. 이 게임의 아이템레벨은 합이고, 승률도
권장 곡선도 전부 그 합을 보므로 한 점짜리 숫자는 어디서도 그대로 쓰이지 않습니다.
문서의 "10티어 +15 = 13,719" 은 코드의 `maxSetIlvl()` 과 같은 값입니다.

## 길드 가입 대기를 없앴다 (2026-08)

가입한 날 하루 동안 출석·레이드·합동사냥을 막던 규칙이 있었습니다. 없앴습니다.

막으려던 것은 **하루치를 두 번 먹는 것**이었습니다 — A길드에서 출석하고 나와서
B길드에 들어가면 출석이 다시 열렸으니까요. 그런데 그건 대기가 아니라 **가입·탈퇴할
때 하루치 기록(`guildCheck`·`guildBoss`)을 지워 버린 것**이 원인이었습니다.
원인은 그대로 두고 첫날 전체를 잠가서 덮은 셈이라, 정직하게 들어온 사람이 하루를
통째로 손해 봤습니다. 길드에 막 들어온 사람이 제일 하고 싶은 게 길드 활동인데요.

지금은 **한도를 옮겨도 들고 갑니다.**

| | 옮길 때 |
|---|---|
| 하루 출석 기록 (`guildCheck`) | 그대로 유지 — 오늘 했으면 옮겨도 막힘 |
| 합동사냥 시도 횟수 | 그대로 유지 |
| 합동사냥 기여도 (`damage`) | 0 으로 — 한도는 사람에게, 기여는 길드에 붙는다 |

⚠ 지우는 자리가 **네 곳**이었습니다 (가입·탈퇴·창설·해산). 가입만 고치면
"탈퇴 후 재가입" 으로 구멍이 그대로 남습니다 — 실제로 그렇게 남아 있었고
스토어 시험이 잡았습니다. 넷을 다 봐야 합니다.

레이드(`raids`)는 원래부터 안 지웠으므로 손댈 곳이 없었습니다.

## 시간 처리

체력(10분/1), 투기장 뱃지(10분/1), 대출 만기(24~72시간), 크리처 러쉬(5분),
체력 회복, 시즌(2주), 퀘스트 리셋(1시간) — 전부 **로컬 시계 기준 경과 시간 계산**입니다.

- 백그라운드 타이머를 쓰지 않고, 복귀 시 `tick()` 이 경과분을 한 번에 정산합니다.
- 크리처 러쉬 대진·선술집 소문은 **시간 슬롯을 시드로 한 결정론적 생성**이라
  앱을 껐다 켜도 같은 결과가 나옵니다. 배팅이 끝난 뒤 결과가 바뀌면 안 되니까요.

⚠️ **기기 시계를 돌리면 뚫립니다.** 프로토타입이라 감수한 부분이고,
정식 서비스로 간다면 서버 시간이 필요합니다.

## 밸런스 실측 (`npm run sim` 결과)

기획서 §13-1 은 **"판매가 = 투자액의 35~45%"** 를 원칙으로 두는데, 표의 수치를
그대로 구현하면 실제로는 그 범위에 들어오지 않습니다.

| 티어 | +0→+15 평균 실투자 | +15 판매가 | 회수율 |
|---|---|---|---|
| 1 | 22실버 70쿠퍼 | 76쿠퍼 | **3.3%** |
| 3 | 9골드 2실버 | 2골드 60실버 | 28.8% |
| 5 | 113골드 26실버 | 32골드 50실버 | 28.7% |
| 8 | 2,270골드 | 555골드 | 24.4% |
| 10 | 13,391골드 | 3,600골드 | 26.9% |

- **회수율이 목표(35~45%)보다 낮은 24~29%** 입니다. 실패·하락·파괴로 실제 투자액이
  이론 강화비보다 훨씬 커지기 때문입니다 (+15 도달까지 **평균 1.95회 파괴**).
- **티어 1이 3.3%로 극단적으로 어긋납니다.** 판매가 표의 1쿠퍼/+5쿠퍼가
  강화비(5쿠퍼×계수) 대비 너무 낮습니다.
- 이론 강화비(실패 무시) 기준으로 보면 49~85%라 오히려 목표보다 높습니다 —
  기획서의 "투자액"이 어느 쪽을 뜻하는지 확정이 필요합니다.

**현재 코드는 기획서 표를 그대로 따릅니다.** 조정하려면 `src/core/tiers.ts` 의
`sellBase` / `sellPerLevel` 만 고치면 되고, `npm run sim` 으로 즉시 재검증됩니다.

### 퀘스트 기대값 (조정 완료)

§7-3 의 승률 공식 `50% + (내 템렙 − 추천 템렙) × 0.5%p` 는 추천 템렙이 내 템렙
상대값이라 **모든 난이도가 항상 50% 근처**가 됩니다. 보상 배수(×1.5~×6.0)와 곱하면:

- 초·중반 매우 어려움 **EV 2.98** — 무한 반복이 최적 전략 (돈 복사)
- 보통은 **영원히 정확히 50%** → 항상 EV 1.10
- 후반 어려움·매우어려움은 승률 5% 클램프 → **죽은 콘텐츠**

**보상 배수의 손익분기 승률에서 출발**하도록 바꿨습니다.

```
기준 승률 = QUEST_HOUSE_KEEP / 보상 배수      // 0.85 / mul
실제 승률 = 기준 승률 + clamp((현재 템렙 / 굴린 시점 템렙 − 1) × 0.5, ±0.20)
```

| 난이도 | 보상 배수 | 손익분기 승률 | 기준 승률 | 기대값 |
|---|---|---|---|---|
| 쉬움 | ×1.5 | 66.7% | 56.7% | **0.85** |
| 보통 | ×2.2 | 45.5% | 38.6% | **0.85** |
| 어려움 | ×3.5 | 28.6% | 24.3% | **0.85** |
| 매우 어려움 | ×6.0 | 16.7% | 14.2% | **0.85** |

전 난이도 기대값이 0.85 로 통일되고(하우스 몫 15%), 난이도는 이제 **위험/보상 성격의
선택**이 됩니다 — 57% 소액이냐 14% 대박이냐. 기준 승률은 보상 배수에서 역산하므로
배수를 바꾸면 자동으로 따라옵니다.

**템렙이 의미를 갖게** 하려고 퀘스트를 스토어에 고정(`questBoard`)했습니다. 굴린 시점의
템렙이 기준선이라, 그 뒤 장비를 갖추면 실제로 쉬워지고 내구도가 닳으면 어려워집니다
(§10 과 자연히 연결됩니다).

| 굴린 시점 대비 내 템렙 | ×0.5 | ×0.8 | ×1.0 | ×1.2 | ×1.5+ |
|---|---|---|---|---|---|
| 보통 난이도 승률 | 18.6% | 28.6% | 38.6% | 48.6% | 58.6% (상한) |

> 남은 EV 이슈: **섯다는 EV 정확히 1.00** 입니다 (승 46.4% × 2배 + 무 7.3% × 1배).
> 기획서 §7-1 이 "딜러 승률 보정: 무보정" 을 명시하므로 그대로 뒀습니다. 1.0 을
> 넘지는 않아 돈 복사는 아니지만, 하우스 엣지가 0 이어서 무한 플레이가 손해를 주지
> 않습니다. 크리처 러쉬는 하우스 마진 8% 로 EV 0.92 입니다.

## 기획서에서 해석이 필요했던 곳

- **아이템레벨 정의**: §3-1 의 평균 대신 **합**을 씁니다 (위 섹션 참고). 기획서에서
  의도적으로 벗어난 유일한 규칙이며, 곡선을 16배로 올려 난이도는 보존했습니다.
- **퀘스트 추천 템렙**: §7-3 표대로 **내 템렙 상대값**입니다 (−20% / ±0% / +15% / +30%).
  절대 하한선을 두면 초반에 "내 템렙 10, 추천 160" 같은 거짓 경고가 떠서, 하한은
  장비 한 점 수준(16)까지만 둡니다.
- **퀘스트 수락 조건**: 아이템레벨은 수락을 **막지 않습니다** — 성공 확률에만 영향을
  줍니다. 막는 건 ① 이미 완료 ② 체력 부족 ③ 보증금 부족 ④ 신용불량(3금융 미상환) 뿐이고,
  버튼이 그 이유를 문장으로 표시합니다.
- **퀘스트 보증금**: §7-3 주석의 "평균 템렙 100 → 보통 30실버"를 앵커로 잡고
  `보증금 = 템렙 × 100쿠퍼 × 난이도계수` 로 역산했습니다. 바닥값을 두지 않아야
  §7-9 "회생 직후 바로 퀘스트 가능"이 실제로 성립합니다 (템렙 0 → 보증금 1쿠퍼).
- **주문서 성공률 배수**: 늘어난 성공률만큼 실패/하락/파괴에서 **비율대로** 차감합니다.
- **크리처 배당**: `1 / 승률` 에 하우스 마진 8% 를 뗐습니다. 승률 60% → ×1.53.
- **장인 무구 판매가/제련**: 표에 없어 티어 10 의 연장선(3,000골드 / +400골드)으로 뒀습니다.
- **강제 처분**: 처분 대금은 전액 채권자에게 가고 플레이어는 아무것도 못 받습니다.
  잔여분도 소멸합니다 — "못 갚으면 그만" 전략을 막는 핵심 규칙이라 그대로 구현했습니다.

## 미구현 / 남은 것

- **채팅 실시간 서버** — `ChatTransport` 인터페이스만 준비된 상태 (mock 구동 중).
  욕설 필터·신고·유저 식별·rate limit 은 서버 붙일 때 같이 설계해야 합니다.
- 사운드 (에셋 없음). 현재는 `expo-haptics` 진동으로 대체.
- 무기별 공격 모션은 스프라이트만 있고 전투 연출 애니메이션은 미연결
  (전투가 확률 계산이라 연출 훅만 있으면 붙습니다).
- 투기장은 고스트 데이터만 (실제 다른 플레이어 매칭 없음).
- 앱 아이콘이 Expo 기본값 — 흑백으로 교체 필요.
- **스프라이트가 전부 코드(ASCII 그리드)** — `docs/ASSET_PROMPTS.md` 의 프롬프트로
  실제 아트를 만들면 `SpriteSheet.tsx`(이미지 + 셀 좌표 렌더러)를 추가해 교체해야 합니다.
